<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
    <mapper namespace="Heart">
    
    <insert id="insert">
    insert into heart(heart_seq, heart_flag, per_seq, writer) values(heart_seq.nextval, default, #{per_seq}, #{writer})
    </insert>
    
    <select id="select" resultType="p.memory.dto.HeartDTO">
    select * from heart where per_seq = #{per_seq}
    </select>
    
    <update id="flagTrue">
     update heart set heart_flag = 1 where per_seq = #{per_seq}
    </update>
    
    <update id="flagFlase">
      update heart set heart_flag = 0 where per_seq = #{per_seq}
    </update>
    
    </mapper>